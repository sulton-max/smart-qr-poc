# v0.5 — <working title: TBD>

*Last updated: 2026-06-23*

**Status:** 🚧 Planning (brainstorm) · **Started:** TBD · **Completed:** —

> v0.5's theme is **pending brainstorm** — scope and iterations not yet committed. This is a scaffold seeded from v0.4 deferrals + the planning backlog; fill it in once the theme is locked.

## Decisions (open)

> Unresolved — settle during the brainstorm before committing scope.

- D1 — Is v0.5 a **deliverable** version (new user capability) or the next **SDK-extraction** version? (v0.4 was an extraction pass → the cycle model suggests v0.5 is a deliverable, but confirm.)
- D2 — Scan-count semantics: raw per-scan vs unique-by-footprint? Footprint (device/IP/UA dedup) conflicts with the no-fingerprint / GWDNBM identity stance — decide before any analytics work.
- D3 — Subscriptions-claim merge semantics: how does a guest's subscription follow them when they sign in on another device? (unique on `user_id` blocks a naive merge; same-device upgrade-in-place is already fine.)
- D4 — Launch wedge: which single feature anchors the pitch (never-expire + cheap routing vs custom-domain vs analytics)?
- D5 — Open spec §13 product shape: single vs custom-domain-first · UZ vs global · abuse-moderation depth · edge-redirect timing · self-host edition.

## Candidate scope (brainstorm — not committed)

> Harvested from v0.4 deferrals + the ordered backlog. Each row is a **candidate** only — pull into iterations once the theme is set. Don't treat as a plan.

| Candidate | Type | Source | Why (1-line) |
|---|---|---|---|
| Claim guest subscription on sign-in | issue | v0.4 deferral + backlog | A guest who subscribed then signs in on another device loses the sub — close the gap auth opened (gated on D3). |
| Refresh account profile on login | idea | v0.4 deferral + backlog | v0.3 captures Google name/avatar once at first sign-in; refresh on each login. |
| Live Stripe test-mode end-to-end | feature | v0.4 log + billing backlog | Billing is built but unproven against real keys + `stripe listen` webhook. |
| Scan analytics | feature | backlog (Scan insights) | Scans over time, unique vs total, by device/country/OS — the obvious next product surface (gated on D2). |
| Backend QR in live preview | issue | backlog (Builder + correctness) | Builder preview is client-side and now differs from the server-rendered asset — render preview from the backend so they match. |
| Request validation + slug uniqueness | feature | backlog (Builder + correctness) | FluentValidation (via the SDK) for create/update; enforce slug uniqueness. |
| Re-enable redirect config cache (+ invalidation) | feature | backlog (Production readiness) | v1.0 reads Postgres directly per scan; restore cache with edit-invalidation when scan volume warrants (`CachedRedirectConfigStore` kept for this). |
| Redis as prod config store | feature | backlog (Production readiness) | Flip from direct-DB; harden the write side. |
| Guest cookie recovery (admin-issued link) | feature | backlog (Accounts) | Lost-cookie recovery: admin mints a one-time owner-key link behind a gate. |

> Scope is intentionally short — only items the planning docs already hint at are listed. Add more during brainstorm rather than inventing here.

## Notes

Seeded from v0.4 deferrals + backlog on 2026-06-23; awaiting brainstorm to commit scope + iterations.
