# v0.5 — Code styling

*Last updated: 2026-06-25*

**Status:** ✅ Complete · **Type:** Feature · **Started:** 2026-06-24 · **Completed:** 2026-06-25

### Iteration 1 — Server-side rendering

- [x] Ability to render a code from one server-authoritative source whose builder preview matches the downloaded asset exactly.

### Iteration 2 — Code styling

- [x] Ability to style a code with module and finder-eye shapes, foreground and background colors with gradients, transparency, and a center emoji.

### Iteration 3 — Design system and builder layout

- [x] Lock a design system — lavender `#D9DDE8` canvas · tinted cards `#EBEEF4` · violet brand · teal accent · Geist / Geist Mono.
- [x] Apply `@theme` + Geist across the app; tune the palette to the Visa reference.
- [x] Dark mode — neutral charcoal tone (no cyberpunk) + header toggle via SDK `ColorModeProvider`.
- [x] Re-skin `RuleBuilder` → dense joined list (SDK `Sortable` drag-reorder); status = chip + dot; adopt all screens (codes list · billing · login).
- [x] SDK extensions consumed (`@wow-two-beta/ui@0.0.62`): `accent` token · `Card ambient` · `Sortable` · `ColorModeProvider`.
- [x] Reorganize the builder into tabs (`Destination · Style · Routing`) + sticky preview.
- [x] Adopt SDK motion (`@wow-two-beta/ui@0.0.64`) — builder tab cross-fade; overlays / menus / dialogs / selects enter and exit via SDK `Presence`.

### Iteration 4 — Verification

- [x] Build both hosts + unit + E2E → all green — 146 (Unit 61 · Integration 18 · E2E 57 · Migrations 10).
- [x] Builder preview == the downloaded SVG byte-for-byte — default + styled (E2E parity).
- [x] Module + finder-eye shapes · solid colors · gradient (linear/radial) · transparent bg · center emoji → render (E2E).
- [x] Style persists + round-trips on create and edit — `StyleJson` write + `CodeDto.Style` + builder prefill.
- [x] Scannability — gradient, transparent, and center-emoji codes decode round-trip (ZXing).
