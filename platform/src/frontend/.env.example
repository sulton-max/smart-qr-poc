# Google OAuth Web client id (public — also used by the backend as the token audience).
# Copy this file to .env.local and set your real id from Google Cloud Console.
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
