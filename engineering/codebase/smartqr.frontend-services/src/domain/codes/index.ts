// codes domain — the QR-code model. Sub-domains: `core` (builder: symbology / style / rules) and
// `content` (per content-type wire shapes + the value ↔ content ops).
export * from "./core";
export * from "./content";
