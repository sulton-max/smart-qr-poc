// presentation/marketing — the public surface: routed pages, footer, and the shared brand `Logo`
// (also used by the app shell). Internal kit (`components`, `data`, `blog`) stays behind this barrel.
export * from "./screens";
export { MarketingFooter } from "./components/MarketingFooter";
export { Logo } from "./components/kit";
