// marketing/components — the shared page kit (sections, cards, brand `Logo`) + the site footer.
// The hero widgets live in `./hero` and are imported directly by the landing page.
export * from "./kit";
export * from "./MarketingFooter";
