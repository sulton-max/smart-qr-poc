// application/codes — stateful codes use-cases (orchestration hooks, view-model mappers).
// Empty for now: the screens keep their inline logic; extract a hook only when a second consumer is real.
export {};
