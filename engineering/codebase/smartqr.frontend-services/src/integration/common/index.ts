// integration/common — the shared HTTP client: config (bases + Google client id), the `{ data }`
// envelope reader, the problem→ApiError builder, and the re-exported `ApiError` type.
export * from "./client";
