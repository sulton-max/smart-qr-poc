# Smart QR — Codebase

The code, and the only place code lives. Single-service shape.

- `smartqr.backend-services/` — .NET (Clean Arch): `smartqr.backend-services.slnx` + projects (`SmartQr.Api`, `SmartQr.Redirect.Api`, `SmartQr.Common*`, `SmartQr.Codes`, `SmartQr.Tests.*`).
- `smartqr.frontend-services/` — React 19 + Vite + Tailwind v4 app (`@wow-two-beta/ui`, pnpm).
