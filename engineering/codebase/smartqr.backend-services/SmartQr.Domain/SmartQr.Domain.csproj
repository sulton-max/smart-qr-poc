<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
    </PropertyGroup>

    <ItemGroup>
        <!-- Entity trait interfaces (IKeyedEntity / IAuditable / IHasTableName) + result-pattern markers. -->
        <PackageReference Include="WoW2.Sdk.Backend.Beta" Version="10.0.42-beta" />
    </ItemGroup>

    <ItemGroup>
        <!-- SDK-generic domain primitives shared across the product (e.g. MobileAppStore) — a shared kernel; Common.Domain has no back-reference, so no cycle. -->
        <ProjectReference Include="..\SmartQr.Common.Domain\SmartQr.Common.Domain.csproj" />
    </ItemGroup>

</Project>
