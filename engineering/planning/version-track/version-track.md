# Smart QR — Version Track

Per-version iteration docs (scope + log). Status table + lifecycle in `../planning.md` § Versions.

Each version under `v{X.Y}/v{X.Y}.md` — `v0.1` (foundation) · `v0.2` (migration layer) · `v0.3` (accounts) · `v0.4` (SDK adoption) · `v0.5` (code styling) · `v0.6`.
