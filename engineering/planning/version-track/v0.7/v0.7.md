# v0.7 — Static content and export

*Last updated: 2026-07-04*

**Status:** 🚧 In progress · **Type:** Feature · **Started:** — · **Completed:** —

### Content model — backend-owned encoding (2026-07-03)

Content is now one polymorphic `CodeContent` value object — domain VO = wire DTO (`[JsonPolymorphic]`, `type` discriminator) = `content_json` jsonb (EF `ValueConverter`). The **backend owns payload encoding** (`CodeContent.Encode()`; the 8 static encoders ported byte-for-byte from the old frontend `contentTypes.ts`), the payload is **derived not stored**, and the frontend sends typed fields with no local encoder — removing the frontend↔backend encoder-drift risk and giving real server-preview parity. Replaces the opaque `ContentSpec {type, fields, payload}`; migration `006-content-model`. Plan: `../../content-model-rewire.md`.

Green after the rewire: **Unit 107 · Integration 18 · E2E 68 · Migrations 10** · frontend typecheck + vitest 9.

### Iteration 1 — Static content types

- [ ] Ability to create a code from a static content type — URL, contact card (vCard), WiFi network, calendar event, location, email, SMS, and phone, plus plain text — each with its own builder form.

### Iteration 2 — Barcodes

- [ ] Ability to create non-QR symbologies (Code 128, EAN, UPC, Data Matrix, PDF417, Aztec) from the builder.

### Iteration 3 — Export and print

- [ ] Ability to download a code as an SVG or PNG file instead of opening it in a browser tab.
- [ ] Ability to get a print-ready code — vector output with a sufficient quiet zone, error-correction headroom, and sizing guidance — that stays valid as later decorations (logo, frames) are added.

### Iteration 4 — Builder correctness

- [ ] Ability to see clear validation and a guaranteed-unique short link when creating or editing a code.
- [ ] Ability to learn what each builder control does from an info popover by its header.
- [x] Ability to preview the exact server-rendered code in the builder so it matches the downloaded asset. *(server-preview parity for static content — the preview endpoint encodes the typed content with the same encoder as the saved asset; `CodePreviewTests.Preview_MatchesSavedImage_ForStaticContent`.)*

### Iteration 5 — Verification

- [x] Create a code from each content type → the scanned payload is correct (URL, vCard, WiFi, calendar, geo, email, SMS, phone, text). *(encoding is backend-owned: per-type encode parity in `CodeContentEncodeTests`, render→ZXing decode in `CodeImageServiceTests`, persist/GET round-trip in E2E `CodesCrudTests`.)*
- [ ] Create each barcode symbology → it renders and scans.
- [ ] Download SVG and PNG → files save (not a new tab); a print-ready export still scans at small print size.
- [ ] Invalid input surfaces validation; a duplicate slug is rejected; the builder preview equals the downloaded asset.

### Iteration 6 — SDK sync (`@wow-two-beta/ui` `0.0.70`)

Adopt the updated SDK + the new frontend model/serialization conventions. Migration guide: `wow-two-sdk-beta.ui/sdk-migration.md`. Behavior-invariant on the frontend; one backend serialization flip.

**Batch 1 — FE envelope + dates (backend-independent, shipped 2026-07-04; FE typecheck + vitest 9 green):**

- [x] Bump `@wow-two-beta/ui` `0.0.68` → `0.0.70` (+ `@js-temporal/polyfill`) + `pnpm install`.
- [x] Envelope: dropped local `ApiSuccess` → `ApiResponse` from `@wow-two-beta/ui/http`; adopted `ApiError` + `ProblemDetails` in the client (thrown with status + parsed problem, validation-message UX preserved).
- [x] Dates: responses parsed via `parseJson` (Temporal reviver at the client boundary) → `CodeDto.createdAt` typed `Temporal.Instant`; calendar `start`/`end` verified to stay wire strings (datetime-local, no `Z` → reviver skips).
- [x] Collections: `Array<>` → `ReadonlyArray<>` on the code DTOs.

**Batch 2 — enum casing flip (cross-stack). Decision: fix the shared SDK method (convention-faithful).**

Mechanic (chosen 2026-07-04): the wire flip runs through the SDK's `AddJsonStringEnums()`, not a smart-qr-local override — `serialization.md` prescribes it. SDK's `Testing/ApiContracts` + `Codes/StyleSpecJson` also use a plain (PascalCase) converter but are storage/test-side + case-insensitive-tolerant → flagged as a separate SDK-consistency follow-up, not in this push.

- [x] **FE flip (done 2026-07-04; typecheck + vitest 9 green):** `BarcodeFormat`/`RuleConditionType`/`Plan` values → camelCase + `{Enum}Labels` (moved to the enum file); `CodeType`/`UserKind` unions → camelCase; `EccLevel` → `l/m/q/h`; deleted `enumFromWire` (direct wire→state); fixed `previewCodeType`/`codeType`/`eccLevel` literals + `AppLayout` `kind` compares. `{Enum}Labels` `Unresolved`-first member **deferred** (would force `Object.values` dropdown filtering — separate small pass).
- [x] **SDK method fixed + published (done 2026-07-04):** `AddJsonStringEnums()` + `ApplySdkJson()` → `JsonStringEnumConverter(JsonNamingPolicy.CamelCase)` + `Json.md` doc → pushed → CI published `WoW2.Sdk.Backend.Beta` `10.0.42-beta`.
- [x] **Phase B done (2026-07-04):** bumped the 10 backend SDK pins `10.0.41` → `10.0.42-beta`; flipped `CodeContentJson.Options` → camelCase converter (persist == wire); build 0 err. **All suites green: Unit 107 · Integration 18 · E2E 68 · Migrations 10.** 10 E2E `Kind`/`Plan` wire-casing asserts flipped to camelCase (request bodies passed untouched — reads are case-insensitive; `status` was already lowercase).
- [ ] Models (remainder): `T?` not `| null` (style/content optionals); bare-model renames + one-type-per-file are a separate frontend-architecture pass.
- [ ] Follow-up (chip spawned): SDK `Testing/ApiContracts` + `Codes/StyleSpecJson` still use PascalCase converters (tolerant/storage-side) — camelCase for full consistency, separate push each.
