# v0.4 — SDK adoption

*Last updated: 2026-06-24*

**Status:** ✅ Complete · **Type:** Adoption · **Started:** 2026-06-19 · **Completed:** 2026-06-23

### Iteration 1 — Host floor

- [x] Adopt `AddApiDefaults`/`UseApiDefaults` for both hosts (Serilog, OTel, health, ProblemDetails, OpenAPI).

### Iteration 2 — Persistence base

- [x] Adopt `AppDbContextBase` + SDK audit interceptor + SDK entity contracts (`IKeyedEntity`/`IAuditable`/`IHasTableName`) + `EnumCaseConverter`.

### Iteration 3 — Presentation and validation

- [x] Adopt SDK trace-aware ProblemDetails, FluentValidation + mediator behavior, and `ApiResponse<T>`; extract the MVC validation filter → SDK.

### Iteration 4 — Identity cookie scheme

- [x] Adopt SDK `AddCookieAuthentication(Mode=Api)`; extract credentialed-CORS → SDK; wire single-host `BuildSpa`.

### Iteration 5 — Test baseline

- [x] Adopt `WoW2.Sdk.Backend.Beta.Testing`, retiring the `SmartQr.Platform.Testing` fork.

### Iteration 6 — Frontend UI library

- [x] Adopt `@wow-two-beta/ui` across screens/marketing/app shell; extract surfaced gaps → `wow-two-sdk-beta.ui`.

### Iteration 7 — Residual infra extraction

- [x] Extract the config-loader and the identity seam (guest-session, current-user, SPA Google ID-token verify) → SDK, and adopt `Data.DatabaseOptions`.

### Iteration 8 — Dead-feature and doc hygiene

- [x] Remove dead `MaxScans`/`PasswordHash`; move entity defaults into command logic; purge `<example>` repo-wide; strip the product env prefix.

### Iteration 9 — Data-tier test infra extraction

- [x] Extract the data-tier test infra (migrator harness, drop-schema fixture, EF-swap helpers) → companion pkg `WoW2.Sdk.Backend.Beta.Testing.Data`, then adopt across smart-qr.
- [x] Extract the test-DB provider switch (`TestSetupOptions`/`RelationalTestDb<T>`) and the host/EF/JSON gaps (`AddPostgresPersistence<T>`, enum-string + DateTimeOffset conversions, `AddJsonStringEnums`) → SDK, then adopt across all 3 apps.

### Iteration 10 — Verification

- [x] Build both hosts + unit + E2E + migrator → all green on the fully-adopted SDK stack (`10.0.37-beta`), including the `Testing.Data` companion and the test-DB provider switch.
- [x] Run the auth flow (guest → Google → claim → cross-device) → covered by the E2E suite.
- [x] Run smart-qr on both providers → PG 88 (52 unit · 26 E2E · 10 migrator) + SQLite 52.
