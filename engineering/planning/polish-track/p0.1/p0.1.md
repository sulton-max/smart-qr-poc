# p0.1 — Create-code builder components

*Last updated: 2026-07-04*

**Status:** In progress — Iter 1 done · **Started:** 2026-07-04 · **Completed:** —

> Behavior-invariant frontend polish. **Iter 2 = whole-frontend reorg** (arch alignment); then one Design-tab accordion per iteration (fill → shape → center). `Captured` / `Cross-cutting` / `Escalate` / `Convention needs` are staging sections (not polish tasks) — they move out as we go.

## Iteration 1 — color picker (done)

- [x] Extracted the swatch trigger into the SDK (`ColorPicker triggerVariant="swatch"`, `@wow-two-beta/ui@0.0.70`).
- [x] Deleted `TileColorPicker`, replaced it in 4 places (`FillControls` ×3, `CreateCodeScreen` ×1). Typecheck clean.

---

## Iteration 2 — Frontend reorganization (arch alignment) (done)

- [x] Flat `src/` → 5 layers (`bootstrap`/`integration`/`domain`/`application`/`presentation`) × slices (`codes`/`identity`/`billing`/`marketing`/`common`) per `architecture.md`; per-slice barrels, `@/`→`src` alias. ~90 files `git mv`'d (history + in-flight edits preserved).
- [x] `api.ts` → `integration/{common,codes,identity,billing}` · `contentTypes.ts` → `domain/codes/content/{registry,operations}` · enums → own files.
- [x] Sub-divided `core` → `design`/`shape`/`routing`/`preview` (noun sub-domains) + a `screens/` folder; barrels + `@/`/barrel imports. Convention updated (`architecture.md`).
- [x] Fixed the Tailwind `@source` path (broke on the `index.css`→`bootstrap/` move → half-styled UI); gotcha documented in `styling.md`. Verified throughout: typecheck 0 err · test 9/9 · vite build ✓.
- follow-ups → Captured: flatten `content/components` for consistency (decision) · ESLint layering rule (no eslint config yet) · UMD `React.*` sweep · minor import-order. Browser smoke pending (auth-gated).

---

## Iteration 3 — Colors & fill accordion

### `components/FillControls.tsx`

- [ ] Consts: `#7c3aed` ×2 → `DEFAULT_GRADIENT_COLOR`; repeated preset-button classes → `PRESET_BUTTON_BASE_CLASS`.
- [ ] Rename map params `a` → `angle`, `r` → `radius`.
- [ ] Dedup the angle + radius preset rows → one `PresetRow`.
- [ ] Extract `GradientControls` (Linear/Radial + presets) + `GradientColorRow` (colors ternary) sub-components.

### Background — lift out of `CreateCodeScreen`

- [ ] Extract `BackgroundControls` from `CreateCodeScreen` (~L345–372; props: `background` · `transparent` + change handlers; state stays in the screen).
- [ ] Reuse `FillControls`' `Row` for it (drop the hand-rolled div) — one shared row pattern.

---

## Iteration 4 — Shape & eyes accordion

### `components/ShapeControls.tsx`

- [ ] Collapse the 3 scattered module maps (const · `MODULE_LABEL` · order) → one control-side table; rename `MODULE_LABEL` (clearer than "module" for a dot).
- [ ] SVG swatch magic numbers → `SWATCH_GEOMETRY` + shared `SVG_SWATCH_PROPS` (`viewBox` / size / `aria-hidden`).
- [ ] Corner-radius ternaries → `CORNER_RADIUS_MAP` + `FINDER_CORNER_RADII` + `FINDER_SWATCH_DIMS`.
- [ ] Dedup Body + Eyes render → one `PresetGrid`; split Eyes → `EyesSection` (flatten nesting).
- [ ] `ShapeCell`: merge `aria-label` + `title` → one `label`. `FinderSwatch`: `dot` → `isDot`.
- [ ] Borders + hairline → a `Divider`; `mb-2` → `Stack` `gap`.

---

## Iteration 5 — Center accordion

### `components/EmojiControls.tsx`

- [ ] Dedup "None" + emoji buttons → one map over `[null, ...EMOJIS]` (an `EmojiButton`).
- [ ] JSDoc `DEFAULT_SIZE = 0.25` (range + meaning); type-annotate `current: string | null`.

---

## Captured — to batch (analysis done, not yet iterated)

**Enum / domain (polish — conform existing types, not features):**
- [ ] `CodeType` bare union (`"qr"|"barcode"|"link"`) → const-object enum per `enums.md` (domain enum → `CodeTypeLabels` + `Unresolved` first). Lands in `domain/codes/core/enums/`.
- [ ] `FillType` — replace the `FILL_SOLID`/`FILL_GRADIENT` UI strings with a `FillType` value-set (`{Solid,Gradient}` const-object, like `ModuleShape`) in `domain/codes/core/`; decide: explicit `fillType` on the style VO vs infer from `gradient !== null`.
- [ ] Enum options descriptor = **value + label** (displayLabel dropped) — keep `enumOptions()` → `{value,label}[]` as the shape.
- [ ] Update the polish-track convention: conforming an existing union→enum / relocating a const IS polish (feature = a NEW capability).

**SDK (`@wow-two-beta/ui` — after arch-align):**
- [ ] `SwapConnector` hover → SDK: a border-reveal variant + a hover-content/`hoverSlot` (icon-swap on hover); then audit other components that can reuse the style (update-once-integrate-everywhere).
- [ ] **Confirmed:** fold `SegmentedControl` → `ToggleButtonGroup variant="segmented"` (it's `ToggleButtonGroup` + `isAttached` + pill classes). Add the variant, migrate usages, deprecate the peer component.
- [ ] UMD `React.*` sweep: `FillControls.tsx:42,65` · `ContentTypeForm.tsx:24` → import named types (convention now in `code-organization.md`).

---

## Renames (behavior-invariant)

- [ ] `CreateCodeScreen` → `CreateCodePage`
- [ ] `RuleBuilder` → `RuleControls`
- [ ] `ContrastHint` → `ContrastCallout`
- [ ] `ContentTypeForm` → `ContentControls`
- [ ] keep as-is: styling + per-type `*Controls` · `QrPreview` (`*Preview`) · `fields.tsx` `*Field` primitives

---

## Backlog — later iterations

> The content-type sub-tree was split since this audit (`content/fields.tsx` + 10 per-type `*Controls` + a thin dispatcher). The `ContentTypeForm` / `MobileAppFields` / `contentTypes` sections below predate that — **re-audit against the new files** before pulling them into an iteration.

### `components/RuleBuilder.tsx`

- [ ] Replace the plain `<button>` (Add rule) with `<Button>` for consistency.
- [ ] Extract the "Add rule" label to a const.
- [ ] Comment the `exhaustive-deps` disable (why `styleKey` stands in for the object).

### `components/QrPreview.tsx`

- [ ] Extract `#71717a` → `ERROR_TEXT_COLOR`; overlay `white/60` / `black/40` → named consts.
- [ ] Drop the redundant `!loading` check in the error branch (already inside the `!loading` path).
- [ ] Comment the `exhaustive-deps` disable.

### `components/ContrastHint.tsx`

- [ ] Name the WCAG constants — luminance coeffs (`0.03928`/`12.92`/`2.4`, `0.2126`/`0.7152`/`0.0722`) + thresholds `CONTRAST_LOW = 3` / `CONTRAST_WARN = 4.5`, JSDoc → WCAG spec. (Naming only — the ratio math is behavior.)
- [ ] Data-drive the 3 contrast alerts → a config array mapped once.
- [ ] Extract `extractForegroundColors(fg, gradient)`; rename `shown` → `ratioFormatted`.

### `components/ContentTypeForm.tsx` — re-audit (now a thin dispatcher → `ContentControls`)

- [ ] Field-kind ternary chain → a `<FieldRenderer kind=… />` helper.
- [ ] Rename `set` → `setValue`, `values` → `fieldValues`.
- [ ] Extract the shared `nativeInput` style → `lib/` `NATIVE_INPUT_STYLES`.

### `components/MobileAppFields.tsx` → `MobileAppControls.tsx` — re-audit

- [ ] Collapse the 3 repeated `FormField`+`TextInput` blocks → `MOBILE_APP_INPUTS.map`.
- [ ] Rename `fallback` → `activeFallback`; extract `getActiveFallback(options, values)`.

### `lib/contentTypes.ts` — re-audit (split into `content/`)

- [ ] Extract the `"wifi"` / `"hidden"` magic strings → consts.
- [ ] Directional rename `buildContent` / `contentToValues`.
- [ ] Document the `BY_ID` index + the `Record<string,unknown>` → `CodeContent` cast.

### `screens/CreateCodeScreen.tsx` — deferred (screen-standardization track)

- [ ] (deferred) Replace the dozens of `useState` slices with a single form-state object / reducer.
- [ ] (deferred) Extract color + fill into their own component.

---

## Cross-cutting (extract once, use across the tree — SDK candidates)

- [ ] **`PresetGrid`** — Shape's `PresetGrid` and Fill's `PresetRow` are the same label + preset-list + cell pattern → one component.
- [ ] **`Divider` / separator** — Shape's hairline + inter-group borders + `half|eighty|full` + percentage sizing → one component; replaces all border-as-separator usage.
- [ ] **No inline hex / style literals** — QrPreview · Fill · Shape · TileColorPicker all bury colors/classes inline → an extract-to-const norm.
- [ ] **`NATIVE_INPUT_STYLES` + `useFieldValues()`** — shared across the content-type controls.
- [ ] **One label/row pattern** — Fill uses a `Row` helper, Shape hand-rolls labels → pick one.

---

## Escalate (behavior / bug / large-extraction — NOT polish → backlog or a version)

- `ShapeControls` — `FinderSwatch` dot-radius `frameRx*0.6` asymmetry: possible visual bug, verify intent.
- `ShapeControls` — eye column reuses one input for two things: behavior bug.
- `FillControls` — `gradient.stops` used without a `length ≥ 2` guard: confirm the backend contract.
- `FillControls` / `EmojiControls` — emoji ↔ foreground-gradient z-order/blend on publish: possible visual conflict.
- `contentTypes` — `buildContent` casts to `CodeContent`: confirm E2E covers the schema contract.
- **Large SDK extractions** (→ a version `Adoption`, not polish): module/finder selectable tiles, module swatch, `PresetGrid`, `Divider`.

---

## Convention needs (spin into their own convention work)

- **git-with-Claude workflow** — on iteration complete: Claude stages the related files + hands you a commit msg → you commit; parallel work stays unblocked, only build-complete code pushes.
- **frontend — gaps + separators over borders** — fold into the frontend conventions.
- **sdk — regex-constrained inputs** — e.g. a color input that only accepts a hex regex; analyze a reusable pattern.
