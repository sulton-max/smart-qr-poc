# p0.1 — Create-code builder components

*Last updated: 2026-07-09*

**Status:** Iter 1–6 done · Iter 7 = tab-view extraction + Content view · Iter 8 = Design view · Iter 9 = Routing view · p0.2 = content-type controls · **Started:** 2026-07-04

Behavior-invariant frontend polish. Iter 2 = whole-frontend reorg · Iter 3 = enum sync; then one Design-tab accordion per iteration (fill → shape → center = Iter 4·5·6). `Captured` / `Cross-cutting` / `Escalate` / `Convention needs` are staging sections (not polish tasks) — they move out as we go.

Task style: one line per task — the *what*, not the *how* (paths, versions, sub-steps live in git).

## Iteration 1 — color picker (done)

- [x] Extracted the swatch trigger to the SDK `ColorPicker`.
- [x] Deleted `TileColorPicker`; adopted the SDK swatch everywhere.

---

## Iteration 2 — Frontend reorganization (arch alignment) (done)

- [x] Reorganized `src/` into Clean-Arch layers × domain slices, per `architecture.md`.
- [x] Split `api.ts`, `contentTypes.ts`, and the enums into per-slice files; added barrels + the `@/` alias.
- [x] Sub-divided `core` (design/shape/routing/preview); moved routed screens into `screens/`.
- [x] Fixed the Tailwind `@source` path broken by the `index.css` move.
- [x] Verify green — typecheck · test 9/9 · build.

---

## Iteration 3 — Enum convention sync (done)

- [x] Rewrote `enums.md` for layers split, nullability, kind, shape and members; updated the `documentation.md` verb table.
- [x] Aligned docs for: `ModuleShape` · `FinderShape` · `BarcodeFormat` · `RuleConditionType` · `GradientType` · `FillType` · `Plan` · `CodeType` · `EccLevel` · `UserKind`.
- [x] Extracted label/swatch maps to `{Enum}Displays`; deleted dead `PlanLabels`.
- [x] Moved `CodeType` · `EccLevel` · `UserKind` to own enum files.
- [x] Verify green — typecheck · test 9/9 · build.
- follow-ups (→ later): content-registry cluster (`ContentTypeId` · `FieldKind` · `ContentMode`); skipped `Status`/`ReturnStatus` (local FSMs).

---

## Iteration 4 — Colors & fill accordion (done)

- [x] Extracted to SDK — generic `ToggleButtonGroup<T>`, `Gradient` companion + `GradientType`, polymorphic `GradientSpec`.
- [x] FE gradient → `Linear`/`Radial` union + companion ops; backend `GradientApiRequest` → polymorphic (fixes radial 400).
- [x] Extracted to SDK — `PresetIconButton` → `OptionTile` · `ControlRow` → `ControlGroup`, and adopted.
- [x] Extracted to SDK — `RadiusGlyph`, and `makeDefaultGradient` → `Gradient.twoStop`; adopted.
- [x] Polished `FillControls` — data-driven preset rows, inlined stop reads, enum-state + `<fieldset>` cleanup; merged `GradientControls` in.
- [x] Aligned `readonly` props across create-code; rewrote `components.md` · `imports.md` · `documentation.md`.
- [x] Verify green — typecheck · test 9/9 · build.

---

## Iteration 5 — Shape & eyes accordion (done)

- [x] Verify (Escalate): `FinderSwatch` `rx = frameRx*0.6` renders fine (not a bug); "eye reuses one input" resolved.
- [x] `ShapeControls` rewritten — Body + Eyes as `ControlGroup` + inline SDK `OptionTile` grids + one vertical `Divider`; dropped local `PresetGrid`/`ShapeCell`/`EyeColumn`.
- [x] `ShapeDisplays` — named the swatch magic numbers; ternaries → `Record`s (`MODULE_CORNER_RX`/`FINDER_FRAME_RX`/`FINDER_PUPIL_ROUNDNESS`); `readonly` + member docs + import order.
- [x] SDK-lifted the glyphs — `FrameGlyph` + `DotsGlyph`/`VerticalBarsGlyph`/`HorizontalBarsGlyph`/`CellsGlyph` → `presentation/display`; `ShapeDisplays` adopts them (`0.0.79`).
- carried → Iter 7: `BackgroundControls` extract from `CreateCodeScreen`.

---

## Iteration 6 — Center accordion → the emoji picker

Grew from a 2-line polish into a full feature + the R1–R5 convention pass. Done end-to-end.

- [x] Domain catalog + headless `useEmojiPicker` + `EmojiControls` shell (nav variants · tile shapes · size tiles · storage seam).
- [x] R1–R5 convention pass + repo-wide SDK-enum adoption (17 files, 0 magic strings).
- [x] A11y — grid `role="listbox"` + roving-tabindex + arrows; `fields.tsx` labels; `role="status"`; marketing `h1`s.
- [x] Extracted the full picker → SDK `@wow-two-beta/ui`; app adopted (`0.0.83`). `EmojiControls` = thin wrapper (broker + size row).
- [x] SDK polish (`0.0.84`) — `label` · `showFirstCategoryWhenRecentsEmpty` · pluggable `scrollThumbColor` · bigger nav icons; app wired.
- [x] `char`→`glyph` — SDK `EmojiSpec.Char`→`Glyph` (`10.0.45-beta`) · smart-qr BE adopted · FE bridge removed.
- [x] Reset the DB (drop `codes`) — old `char` rows won't bind. Done.
- [ ] Perf (not blocking) — code-split the catalog + virtualize the grid (SDK-side).
- [ ] Future (not polish) — `DesignPhotoOverlay` / photo support.

---

## Iteration 7 — Tab views + Content view

Split `CreateCodeScreen`'s 3 inline tabs into their own components, then polish the Content one first. Naming = **View** (per `components.md` §1 — "content area inside a page, swaps on nav, layout persists"), not `*Controls` (those are the leaf input groups). Per-type content controls (`UrlControls`…`CalendarControls`) polish = **p0.2**.

- [ ] Extract `ContentView` · `DesignView` · `RoutingView` from `CreateCodeScreen` — each `{tab === …}` block → its own component; the `Card` + tab-strip stays in the screen.
- [ ] **Content view polish** (issues found):
  - [ ] URL special-casing — `values={typeId === Url ? { url: fallbackUrl } : contentValues}` + a branched `onChange` leak into the render; unify URL as a normal content type (value in `contentValues`, `fallbackUrl` derived).
  - [ ] `o.itemKey as ContentTypeId` cast on the type `Select` → a typed `Select<ContentTypeId>`.
  - [~] Short-link gate — `"static"` literal → `ContentMode` enum **done (sweep)**; still open: inline `isEdit && existing && mode !== ContentMode.Static` → a named predicate.
  - [ ] Content-tab state → pass `name`/`contentTypeId`/`contentValues`/`fallbackUrl` as one grouped prop, not 4 loose `useState` threads (ties into the form-state-reducer backlog).
- [ ] Analyze `domain/codes/core` (enums + `types.ts`) → propose a correct split (e.g. `style` / `routing` / `code`) before more views harden around it.

---

## Iteration 8 — Design view polish

Review the design components (`FillControls` · `ShapeControls` · `EmojiControls` · `ContrastCallout` · `QrPreview`) + their `DesignView` usage, then polish.

- [ ] Review → polish list.
- [ ] `BackgroundControls` extract (carried from Iter 4/5) — bg color/fill out of `DesignView` → a `ControlGroup`.
- [ ] `*Page` renames — `CreateCodeScreen→CreateCodePage` · `CodesListScreen→CodesListPage`.
- [ ] Open cross-cutting (§ below) — `Divider` · no-inline-hex · one label/row.

---

## Iteration 9 — Routing view polish

- [ ] Review `RoutingView` + `RuleControls` → polish list.

---

## Renames (behavior-invariant)

- [x] `RuleBuilder`→`RuleControls` · `ContrastHint`→`ContrastCallout` · `ContentTypeForm`→`ContentTypeControls`.
- [ ] `*Page` renames → Iteration 8.
- keep as-is: per-type `*Controls` · `QrPreview` (`*Preview`) · `fields.tsx` `*Field` primitives.

---

## Backlog — later

Design-component backlog cleared in the Iter-6 sweep — `RuleControls` (add-rule `<Button>` + `AddRuleLabel`), `QrPreview` (error/scrim consts + exhaustive-deps comment), `ContrastCallout` (WCAG consts + data-driven `alerts` + `extractForegroundColors`) are done.

### Content-type controls → p0.2 (not p0.1)

- [ ] `ContentTypeControls` — field-kind ternary → a `<FieldRenderer kind=…>`; `set`→`setValue`, `values`→`fieldValues`; UMD `React.*` → named import.
- [ ] `MobileAppControls` — collapse repeated `FormField`+`TextInput` → `MOBILE_APP_INPUTS.map`; `fallback`→`activeFallback`.
- [ ] `content/contentTypes` — `"wifi"`/`"hidden"` literals → consts; `buildContent`/`contentToValues` directional rename; document the `BY_ID` → `CodeContent` cast.

### `CreateCodeScreen` — deferred (screen-standardization track)

- [ ] Replace the many `useState` slices with one form-state object / reducer.

---

## Captured — to batch

- [ ] SDK enum-display descriptor `{value,label}[]` (`enumOptions()`) — build once several apps converge on `{Enum}Displays`.
- [ ] UMD `React.*` sweep (design side) — `FillControls.tsx:42,65` → named import.

---

## Cross-cutting (extract once — SDK candidates)

- [ ] `Divider` / separator — Shape hairline + inter-group borders + percentage sizing → one component.
- [ ] No inline hex / style literals — remaining in Fill / Shape (QrPreview + ContrastCallout done).
- [ ] One label/row pattern — Fill's `Row` helper vs Shape's hand-rolled labels → pick one.

---

## Escalate (verify / bug / large-extraction — NOT polish)

- [ ] `FillControls` — `gradient.stops` used without a `length ≥ 2` guard: confirm the backend contract.
- [ ] `FillControls` / `EmojiControls` — emoji ↔ foreground-gradient z-order/blend on publish: possible visual conflict.
- [ ] `contentTypes` — `buildContent` cast to `CodeContent`: confirm E2E covers the schema contract.

---

## Convention needs (spin into their own convention work)

- **git-with-Claude workflow** — on iteration complete: Claude stages the related files + hands you a commit msg → you commit; parallel work stays unblocked, only build-complete code pushes.
- **frontend — gaps + separators over borders** — fold into the frontend conventions.
- **sdk — regex-constrained inputs** — e.g. a color input that only accepts a hex regex; analyze a reusable pattern.
