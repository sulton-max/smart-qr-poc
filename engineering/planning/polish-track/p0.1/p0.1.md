# p0.1 — Create-code builder components

*Last updated: 2026-08-14*

**Status:** In-Progress

## Iteration 1 — Color picker

- [x] Extract the swatch trigger to the SDK `ColorPicker`
- [x] Delete `TileColorPicker`
- [x] Adopt the SDK swatch everywhere

---

## Iteration 2 — Frontend reorganization

- [x] Reorganize `src/` into Clean-Arch layers × domain slices
- [x] Split `api.ts` · `contentTypes.ts` · the enums per slice
- [x] Add the barrels and the `@/` alias
- [x] Sub-divide `core`
- [x] Move the routed screens into `screens/`
- [x] Fix the Tailwind `@source` path

---

## Iteration 3 — Enum convention sync

- [x] Rewrite `enums.md`
- [x] Update the `documentation.md` verb table
- [x] Align the docs for the code enums
- [x] Extract the label/swatch maps to `{Enum}Displays`
- [x] Move `CodeType`·`EccLevel`·`UserKind` to own files

---

## Iteration 4 — Colors & fill accordion

- [x] Extract `ToggleButtonGroup<T>` to the SDK
- [x] Extract `Gradient` + `GradientType` to the SDK
- [x] Extract `OptionTile` + `ControlGroup` to the SDK
- [x] Extract `RadiusGlyph` to the SDK
- [x] Convert the FE gradient to a `Linear`/`Radial` union
- [x] Make the backend `GradientApiRequest` polymorphic
- [x] Merge `GradientControls` into `FillControls`
- [x] Run the `readonly` sweep
- [x] Rewrite `components.md` · `imports.md` · `documentation.md`

---

## Iteration 5 — Shape & eyes accordion

- [x] Rewrite `ShapeControls`
- [x] Name the magic numbers in `ShapeDisplays`
- [x] Convert the `ShapeDisplays` ternaries to `Record`s
- [x] Mark the `ShapeDisplays` maps `readonly`
- [x] Lift the shape glyphs to the SDK
- [x] Adopt the SDK shape glyphs in `ShapeControls`

---

## Iteration 6 — Center accordion → emoji picker

Grew into a full feature + the R1–R5 convention pass.

- [x] Build the emoji domain catalog
- [x] Build the headless `useEmojiPicker`
- [x] Extract the full picker to the SDK
- [x] Adopt the SDK picker in `EmojiControls`
- [x] Run the R1–R5 convention pass
- [x] Adopt the SDK enums repo-wide
- [x] Run the a11y pass
- [x] Rename `char` → `glyph` across SDK, BE and FE
- [x] Reset the DB
- [x] Ship the SDK `Divider`

---

## Iteration 7 — Tab views + slice reshape

- [x] Extract `ContentView` from `CreateCodeScreen`
- [x] Extract `DesignView` from `CreateCodeScreen`
- [x] Extract `RoutingView` from `CreateCodeScreen`
- [x] Extract `PreviewView` from `CreateCodeScreen`
- [x] Dissolve `core/` into the `domain/codes` sub-slices
- [x] Reshape `presentation/codes` for its sub-domains
- [x] Reshape the billing, identity and marketing subdomains
- [x] Type the content-view `Select<ContentTypeId>`
- [x] Add the `isDynamicType` predicate
- [x] Fix the `Select` label-key bug

---

## Iteration 8 — Content view + content controls

Typed content model per type (mirror backend `content/types/*`) — each control bound to its `XContent`; the builder holds `CodeContent` directly (no flat bag).

- [x] Lay out static → `Divider` → dynamic in the content view
- [x] Source `name` from `CodeDto`
- [x] Bind each `*Controls` to its `XContent`
- [x] Drop the flat `FieldValues` + `build`/`parse`
- [x] Hold the content as one `CodeContent`
- [x] Reset the content on type-switch
- [x] Keep `ContentTypeControls` separate from `ContentView`
- [x] Rename `existing` → `existingCode`
- [x] Align the content-model interface docs
- [x] Inline the `TextField` pure-wrap into the controls
- [x] Delete the dead `FieldValues` type

---

## Iteration 9 — Adopt SDK form atoms

- [x] Inline `TextField`
- [x] Switch the `type` discriminant to `typeof ContentTypeId.X`
- [x] Drop the dead `FieldValues`
- [x] Adopt the SDK `TextAreaInput`
- [x] Drop the native `<textarea>`
- [x] Adopt the SDK `UrlInput` · `EmailInput` · `TelInput`
- [x] Delete `useNativeFieldProps`
- [x] Adopt `Select.options` in `SelectField`

---

## Iteration 10 — Datetime consolidation

- [x] Add the SDK `DateTimeField` atom
- [x] Adopt it in `CalendarControls`
- [x] Delete the bespoke `DateTimeField`
- [x] Delete `NativeInputStyles`
- [x] Unify the temporal polyfill on `temporal-polyfill`

---

## Iteration 11 — Code models

- [x] Analyze all layer models
- [x] Relocate domain models → `domain/codes/*/models/`
- [x] Relocate content models → `domain/codes/content/models/`
- [x] Relocate write requests → `integration/codes/models/`
- [x] Relocate the form + mappers → `application/codes/`
- [x] Rename `symbology` → `barcodeFormat`
- [x] Slim the `ContentTypes` registry
- [x] Delete the essay block comments repo-wide
- [x] Relocate the tests → `tests/` mirroring src
- [x] Eliminate the `*FormValues` type
- [x] Extract the `ContentType` enum → `ContentTypeDescriptor`
- [x] Extract `registry.ts` → `contentTypeCatalog`
- [x] Sweep the frontend model docs `Gets or sets` → `The …`
- [x] Split `integration/codes/models/` → one file per request
- [x] Build `codesApiClient` from `integration/codes/codes.ts`
- [x] Keep `CodeSetActiveApiRequest` as a Set request
- [x] Regroup the enums by role under `enums/`
- [x] Cut the enum docs to a one-liner
- [x] Reword the `*Content` docs to `Represents`

---

## Iteration 12 — Model refinements

Every model earns its members; every offered control is real. (Renamed 2026-07-17 from "Trim down" — trim dropped: the built types/conditions would only return post-polish, and the unbuilt items have no surface to cut.)

**Done:**

- [x] Drop `CodeType`
- [x] Make `BarcodeFormat` the sole symbology
- [x] Fix non-QR formats rendering as `Code128` in the preview
- [x] Convert `fallbackUrl` to an optional `Default` rule
- [x] Return 404 on no-match
- [x] Drop the vestigial `NeverExpires`/`ExpiresAt` expiry
- [x] Make `content` non-nullable
- [x] Delete the content-spec system
- [x] Dissolve defects D2 · D3 · D7
- [x] Move D1 · D4 · D5 · R2 to v0.9

Open: D8's last fallback-copy site, `PreviewView.tsx:15`, waits — it is v0.9's staged file. R5 is deferred, the async `scanCount` denorm counter deriving from `scan_events` later. `IGeoResolver` is stubbed `NoopGeoResolver`, so `Country` / `Language` rules never match. Center logo · file upload · local files are unbuilt — deferred, not trimmed; each lands with its own version.

**Open (p0.1):**

- [ ] Purge the last fallback copy from `PreviewView`
- [ ] Keep the async `scanCount` denorm counter
- [ ] Replace the `NoopGeoResolver` stub
- [ ] Defer center logo · file upload · local files

> Content-model work — M1a validator restructure · the Default circle · content validation — moved to **v0.9** (CM15 rule-carries-content). Detail: `validation.md` · `v0.9.md` § *Lane split* · git history.

---

## Iteration 13 — Dynamic vs static content mode → v0.9

**Done elsewhere.** Became `version-track/v0.9/v0.9.md` (content mode); v0.9 is building it. Empty slot kept so Iter 14/15 keep their numbers.

---

## Iteration 14 — Resolve `SelectField`

> **Calendar datetime removed 2026-07-22 → v0.9.** `CalendarContent.start/end` are already `Temporal.PlainDateTime` (v0.9 sweep, `v0.9.md` Iter 2 ✓). The `CalendarControls` bridge reconcile sits in v0.9's staged work (4 typecheck errors there). Not p0.1's.

The lone remaining atom decision — no content-model dependency, so it's genuinely p0.1's.

`SelectField` lives in `presentation/codes/content/components/fields.tsx`; deleting it depends on SDK `Select.options` rendering the list itself.

- [ ] Resolve `SelectField`
  - [ ] decide: extract to the SDK, or delete and inline it
  - [ ] apply the decision

---

## Iteration 15 — Test naming

Form: **`{Method}_{When}_{Assertion}`** — each section PascalCase, underscores only between sections. `Scan_IosDevice_RedirectsToRuleDestination` ✓ · `Ios_scan_routes_to_rule` ✗.

> **Scoped 2026-07-17.** Two of four suites conform already — the sweep is Unit + Integration only.

| Suite | Style today | Action |
|---|---|---|
| `SmartQr.Tests.E2E` | `Checkout_FreePlan_Returns400` | conforms — leave |
| `SmartQr.Tests.Migrations` | `ApplyPending_TwoRunnersConcurrently_AppliesExactlyOnce` | conforms — leave |
| `SmartQr.Tests.Unit` | `Dynamic_types_bake_no_payload` | **sweep** |
| `SmartQr.Tests.Integration` | `Ios_scan_routes_to_rule` | **sweep** |

The convention lands in `wow-two-ws/conventions/` — a separate git, so it commits there, not here. Two names assert retired behaviour: `Scan_DesktopDevice_WithNoMatchingRule_RedirectsToFallback` still says fallback although R3 made no-match a **404**, and `Dynamic_types_bake_no_payload` asserts the `Encode()`-nullity model that `v0.9` overturns — recheck it against the mode axis before renaming.

- [ ] Sweep `SmartQr.Tests.Unit` → `{Method}_{When}_{Assertion}`
- [ ] Sweep `SmartQr.Tests.Integration` → `{Method}_{When}_{Assertion}`
- [ ] Add the test-naming convention to `wow-two-ws/conventions/`
  - [ ] state the 3-section form + PascalCase-per-section
  - [ ] define `{Method}` as the unit under test
  - [ ] cite the two conforming suites as the reference
- [ ] Fix names that assert retired behaviour while sweeping
  - [ ] rename the desktop no-match redirect test
  - [ ] recheck `Dynamic_types_bake_no_payload` before renaming

---

## Renames

- [x] Rename `RuleBuilder` → `RuleControls`
- [x] Rename `ContrastHint` → `ContrastCallout`
- [x] Rename `ContentTypeForm` → `ContentTypeControls`
- [x] Rename `existing` → `existingCode`
- keep as-is: per-type `*Controls` · `QrPreview`.

The `*Page` renames + the open staging sections → **[p0.2](../p0.2/p0.2.md)**.
