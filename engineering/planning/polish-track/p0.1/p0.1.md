# p0.1 — Create-code builder components

*Last updated: 2026-07-11*

**Status:** In-Progress

## Iteration 1 — Color picker

- [x] Extracted the swatch trigger to the SDK `ColorPicker`.
- [x] Deleted `TileColorPicker`; adopted the SDK swatch everywhere.

---

## Iteration 2 — Frontend reorganization

- [x] Reorganized `src/` into Clean-Arch layers × domain slices, per `architecture.md`.
- [x] Split `api.ts`, `contentTypes.ts`, and the enums into per-slice files; added barrels + the `@/` alias.
- [x] Sub-divided `core` (design/shape/routing/preview); moved routed screens into `screens/`.
- [x] Fixed the Tailwind `@source` path broken by the `index.css` move.

---

## Iteration 3 — Enum convention sync

- [x] Rewrote `enums.md` for layers split, nullability, kind, shape and members; updated the `documentation.md` verb table.
- [x] Aligned docs for the code enums; extracted label/swatch maps to `{Enum}Displays`; moved `CodeType`·`EccLevel`·`UserKind` to own files.

---

## Iteration 4 — Colors & fill accordion

- [x] SDK extractions — `ToggleButtonGroup<T>` · `Gradient`/`GradientType` · `OptionTile` · `ControlGroup` · `RadiusGlyph`
- [x] FE gradient → `Linear`/`Radial` union; backend `GradientApiRequest` polymorphic (fixes radial 400)
- [x] Polished `FillControls` (merged `GradientControls` in); `readonly` sweep; rewrote `components.md`·`imports.md`·`documentation.md`

---

## Iteration 5 — Shape & eyes accordion

- [x] `ShapeControls` rewritten — Body + Eyes via `ControlGroup` + SDK `OptionTile` grids
- [x] `ShapeDisplays` — named magic numbers, ternaries → `Record`s, `readonly`
- [x] SDK-lifted the shape glyphs; `ShapeControls` adopts them

---

## Iteration 6 — Center accordion → emoji picker

Grew into a full feature + the R1–R5 convention pass.

- [x] Domain catalog + headless `useEmojiPicker`; extracted the full picker → SDK, app adopted (`EmojiControls` = thin wrapper)
- [x] R1–R5 convention pass + repo-wide SDK-enum adoption (0 magic strings); a11y pass
- [x] `char`→`glyph` across SDK + BE + FE; DB reset · SDK `Divider` shipped

---

## Iteration 7 — Tab views + slice reshape

- [x] Extract `ContentView` · `DesignView` · `RoutingView` · `PreviewView` from `CreateCodeScreen`
- [x] Reshape `domain/codes` — dissolve `core/` → `common` · `content` · `style` · `rules`
- [x] Reshape `presentation/codes` for `content` and other sub-domains
- [x] Reshape billing, identity, marketing subdomains for domain vs kind-folder mixup
- [x] Content view — typed `Select<ContentTypeId>` + `isDynamicType` predicate
- [x] Fix `Select` component label-key bug

---

## Iteration 8 — Content view + content controls

Typed content model per type (mirror backend `content/types/*`) — each control bound to its `XContent`; the builder holds `CodeContent` directly (no flat bag).

- [x] Layout — static (name · type) → `Divider` → dynamic (short link · fields)
- [x] `name` from `CodeDto` (code identity), separate from content
- [x] Typed content model per type — each `*Controls` binds its `XContent`; dropped the flat `FieldValues` + `build/parse`; content = one `CodeContent`; `emptyContent(id)` on type-switch
- [x] `ContentTypeControls` kept separate from `ContentView`
- [x] `existing`→`existingCode`; content-model interfaces aligned to the `Defines`/`Gets or sets`/blank-line convention
- [x] Inlined the `TextField` pure-wrap into the controls; removed dead `FieldValues`

---

## Iteration 9 — Adopt SDK form atoms

- [x] Inline `TextField`; `type` discriminant → `typeof ContentTypeId.X`; drop dead `FieldValues`
- [x] Adopt SDK `TextAreaInput`; drop the native `<textarea>`
- [x] Adopt SDK typed inputs `UrlInput`/`EmailInput`/`TelInput` (Geo keeps `TextInput`)
- [x] Delete `useNativeFieldProps` — SDK atoms auto-wire to `Field`
- [x] Adopt `Select.options` in `SelectField`

---

## Iteration 10 — Datetime consolidation

- [x] Add SDK `DateTimeField` atom (`datetime-local` + `Temporal.PlainDateTime`, mirrors `DateField`)
- [x] Adopt it in `CalendarControls`; delete bespoke `DateTimeField` + `NativeInputStyles`
- [x] Unify temporal polyfill → `temporal-polyfill`

---

## Iteration 11 — Code models

- [x] Analyze all layer models
  - [x] map every code / creation / form model → its layer (`domain` · `application` · `integration`)
  - [x] lock the model-name scheme (`*Dto` family + `*Request`) + file granularity + `models/` role-group
- [x] Domain models → `domain/codes/*/models/` (entity + shared nested `*Dto`)
  - [x] relocate + name: `CodeDto` (common) · inline rule → `CodeRuleDto` (rules) · `PreviewStyle`→`CodeStyleDto` + `PreviewLogo`→`CodeLogoDto` + `DesignEmojiOverlay`→`CodeEmojiDto` (style); gradient stays SDK `Gradient`
  - [x] docs: `Defines`/`Represents` type + member docs; kill essay headers *(member keyword → `The …` sweep below)*
  - [x] members: one blank line each; `?` optional, never null
- [x] Content models → `domain/codes/content/models/`
  - [x] relocate `content/types/*` → `content/models/*`; keep `*Content` + union `CodeContent`
  - [x] docs + members verify (largely aligned in Iter 8)
- [x] Write requests → `integration/codes/models/` (top-level `*ApiRequest` only)
  - [x] relocate + merge: `Create`/`Update` → `CodeCreateUpdateApiRequest` · `CodeSetActiveApiRequest` · `CodePreviewApiRequest`
  - [x] nested props reference the domain `*Dto` (`CodeRuleDto` · `CodeStyleDto`) — **not** `*ApiRequest`
  - [x] name `*ApiRequest` noun-first; docs + members (frontend convention)
- [x] Form + mappers → `application/codes/`
  - [x] form binds `CodeCreateUpdateApiRequest` — `CreateCodeValues` + `BuilderStyle` deleted
  - [x] `CreateCodeSchema` + the edit-prefill mapper (`CodeDto` → `CodeCreateUpdateApiRequest`) → `application`; dissolve `aggregate.ts`
- [x] `symbology` → `barcodeFormat` *(codeType-drop → Iter 13)*
- [x] Slim the `ContentTypes` registry
  - [x] drop dead `fields`/`FieldKind` *(extract enum + rename → below)*
- [x] Delete essay block comments repo-wide
- [x] Tests → `tests/` mirroring src (+ vitest `include` + convention)
- [x] Eliminate the `*FormValues` type — form binds `CodeCreateUpdateApiRequest` directly
  - [x] 2 bind cases only: submit → `*ApiRequest`, in-frontend update → `*Dto`
  - [x] rule-row key via the SDK form-array's `row.key` (not a modeled `id`); partial-scheme validation
  - [x] keep the `emptyX…` init-values const
- [x] Extract `ContentType` enum → `content/ContentType.ts`; `registry.ts` → `content/models/ContentTypeDescriptor.ts` — `ContentTypeDescriptor` entry + `contentTypeCatalog` + `contentType(id)` lookup
- [x] Sweep frontend model member docs `Gets or sets` → `The …` (14 files)
- [x] Split `integration/codes/models/` → one file per request (distinct endpoints)
- [x] `integration/codes/codes.ts` → `codesApiClient` — compound methods (8: `create`·`list`·`get`·`update`·`setActive`·`delete`·`imageUrl`·`preview`)
- [x] `CodeSetActiveApiRequest` — confirmed: keep Set (carries `isActive`; a toggle would be bodyless)

---

## Iteration 12 — Select field + calendar datetime

- [ ] Resolve `SelectField`
  - [ ] decide: extract to SDK, or SDK `Select options` renders the list → delete + inline
  - [ ] apply
- [ ] Calendar datetime
  - [ ] `CalendarContent.start`/`end` `string` → `Temporal.PlainDateTime`
  - [ ] `toCreateCodeRequest` formats → ISO at the wire; drop the `CalendarControls` bridge

---

## Iteration 13 — CodeType vs BarcodeFormat

- [ ] Analyze a possible merge
  - [ ] `CodeType` (`Qr`/`Barcode`/`Link`) vs `BarcodeFormat` (symbology) — `CodeType` derivable
  - [ ] decide: derive `CodeType` from format vs keep both

---

## Renames

- [x] `RuleBuilder`→`RuleControls` · `ContrastHint`→`ContrastCallout` · `ContentTypeForm`→`ContentTypeControls` · `existing`→`existingCode`.
- keep as-is: per-type `*Controls` · `QrPreview`.

The `*Page` renames + the open staging sections → **[p0.2](../p0.2/p0.2.md)**.
