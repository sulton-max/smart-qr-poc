# p0.3 — Backend model alignment

*Last updated: 2026-07-22*

**Status:** In-Progress

Align the backend code models (`SmartQr.*`) with the frontend naming + doc conventions — mirror shape/names so both sides read the same. Frontend-side moves live in [p0.1](../p0.1/p0.1.md).

## Iteration 1 — Request models

- [ ] Rename inconsistent `*Request` → `*ApiRequest` (unify the pattern)
- [ ] Adopt noun-first `{Noun}{Verb}ApiRequest`; merge create+update → `CodeCreateUpdateApiRequest` (id via route)
- [ ] Nested body models → `*Dto` (not `*ApiRequest`): `RuleApiRequest`→`CodeRuleDto` · `StyleApiRequest`→`CodeStyleDto` · `Logo`/`Gradient`/`Emoji`ApiRequest → `*Dto`; only the top-level request keeps `*ApiRequest`

## Iteration 2 — Content polymorphism

- [ ] `CodeContentType` — fix type + member docs (`Represents` / `Gets`, per doc conventions)
- [ ] Enum-driven discriminator resolver `CodeContentPolymorphism` — **landed + 22/22 green** (replaced the `[JsonDerivedType]` magic strings; fixed the `nameof` Url bug; `VCard` kept `"vcard"` via `[JsonStringEnumMemberName]`)
  - [ ] apply the same resolver to `GradientApiRequest` (linear/radial)
  - [ ] extract the reusable resolver → `WoW.Two.Sdk.Backend.Beta` (polymorphism-by-enum)
  - [ ] decide: keep `VCard = "vcard"` vs align to the consistent `"vCard"` (needs a frontend + stored-data change)
- [ ] all content models (`UrlContent` / `WifiContent` / …) — fix type + member docs

## Iteration 3 — Dynamic-by-default

- [x] Remove `IsStatic` — **DONE 2026-07-22 by the v0.9 models sweep.** `IsStatic` + `isDynamicType`/`isDynamicContent` deleted; replaced by an explicit `CodeEntity.Mode` (`ContentMode`). Bakeability is now `f(content, rules)`, resolved server-side — not a content property.
