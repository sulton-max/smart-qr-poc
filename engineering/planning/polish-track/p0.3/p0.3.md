# p0.3 — Backend model alignment

*Last updated: 2026-08-14*

**Status:** In-Progress

Align the backend code models (`SmartQr.*`) with the frontend naming + doc conventions — mirror shape/names so both sides read the same. Frontend-side moves live in [p0.1](../p0.1/p0.1.md).

## Iteration 1 — Request models

The pattern: only the top-level request keeps `*ApiRequest`, nested body models become `*Dto`. The merged create + update carries the id on the route.

- [ ] Rename the inconsistent `*Request` → `*ApiRequest`
- [ ] Adopt the noun-first `{Noun}{Verb}ApiRequest` shape
- [ ] Merge create + update → `CodeCreateUpdateApiRequest`
- [ ] Rename `RuleApiRequest` → `CodeRuleDto`
- [ ] Rename `StyleApiRequest` → `CodeStyleDto`
- [ ] Rename the `Logo`/`Gradient`/`Emoji` requests → `*Dto`

## Iteration 2 — Content polymorphism

Doc fixes follow the doc conventions — `Represents` on the type, `Gets` on the members. `VCard` serializes as `"vcard"` via `[JsonStringEnumMemberName]` — aligning it to `"vCard"` needs a frontend + stored-data change.

- [ ] Fix the `CodeContentType` type and member docs
- [x] Build the `CodeContentPolymorphism` resolver
  - [ ] apply the resolver to `GradientApiRequest`
  - [ ] extract the resolver → `WoW.Two.Sdk.Backend.Beta`
  - [ ] decide: keep `VCard = "vcard"` or align to `"vCard"`
- [ ] Fix the content-model type and member docs

## Iteration 3 — Dynamic-by-default

- [x] Remove `IsStatic`
