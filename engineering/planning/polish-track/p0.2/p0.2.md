# p0.2 — Create-code deep polish

*Last updated: 2026-07-10*

**Continues [p0.1](../p0.1/p0.1.md)** (Iters 1–9 = structural + wrapper/SDK polish). p0.2 opens with **content-type components polish** — each content type = its own iteration = a full design pass (mocks + philosophy, like the Design tab, not just a rewrite) + component + backend fix. Then the Design & Routing views, then validation.

Task style: one line per task — the *what*, not the *how*.

---

## Iteration 1 — Content-type components polish (per content type)

Each type gets its own sub-pass (design mocks + philosophy + component + backend fix). Known so far:

- [ ] **Calendar** — QR doesn't scan on phone (content / backend iCalendar encoding)
- [ ] **MobileApp** — rule-builder-style add/remove link inputs (UX redesign); single-source labels from the registry; `FALLBACK_KEY` const; string-union `key` → const/derived-from-model
- [ ] **Per model** — revisit gloss/shape (e.g. mobileApp `other` — "fallback" reads off) + design mocks
- [ ] Control cleanup (from p0.1 Iter 8) — label single-sourcing (inline vs registry `fields`); field-kind → input `type`/`inputMode` (Tel/Email/Url/Number — mild behavior, confirm); type-switch **warn-on-save** in edit mode; `registry` literals → consts + `readonly`

---

## Iteration 2 — Design view polish

- [ ] Review the design components (`FillControls` · `ShapeControls` · `EmojiControls` · `ContrastCallout` · `QrPreview`) + `DesignView` → polish list.
- [ ] `BackgroundControls` extract — bg color/fill out of `DesignView` → a `ControlGroup`.
- [ ] `*Page` renames — `CreateCodeScreen→CreateCodePage` · `CodesListScreen→CodesListPage` (+ unify marketing `*Page` file/folder naming; `screens/`→`pages/`?).
- [ ] Open cross-cutting (§) — `Divider` · no-inline-hex · one label/row.

---

## Iteration 3 — Routing view polish

- [ ] Review `RoutingView` + `RuleControls` → polish list.

---

## Iteration N — Form validation (after the forms are polished)

Validate the *final* shape. SDK is building plug-and-play validation (another chat) → **review + adopt**, don't roll our own.

- [ ] Single validation entry — each `*Content` provides its own schema, composed into a **discriminated union over `type`**; `validateContent(content)` dispatches; wire the SDK `Field` error slots + submit gate; lift the ad-hoc mobileApp "at least one link" guard in.

---

## SDK adoption + gaps

- [ ] **Upgrade router + queries** from the SDK (later) — adopt the SDK's routing + data-query layer in place of the app's current wiring.
- [ ] `Select.options` should **render the dropdown items** (data-driven), not only seed labels — then `SelectField` collapses to `<Field><Select options/></Field>` and inlines away.
- [ ] Combined `dateTimePicker` atom — the one datetime piece the SDK lacks (it has separate `dateField`/`timeField`); add it if composing date+time gets repetitive.
- [ ] SDK — regex-constrained inputs (e.g. a hex-only color input); reusable pattern.

---

## Convention needs

- **frontend — wrap philosophy** — inline pure prop-pass compositions (Tailwind-style); extract only when the wrapper adds logic (e.g. `TextAreaField`'s native-element `Field` wiring). Codify.
- **frontend — presentation grouping** — codify the kind-folder rule (`components`/`screens`/`views`; features under `common/`; no domain ↔ kind-folder mixing). Sub-question: "mirrors the page's views" vs "mirrors domain slices".
- **frontend — view prop-model** — views explode toward 50+ props (`DesignView` ~18); a state-model prop (or two-way binding) over N flat props.
- **frontend — gaps + separators over borders** · **git-with-Claude workflow** (Claude stages + drafts msg → dev commits).

---

## Cross-cutting · Escalate · Captured (moved from p0.1)

- [ ] **Cross-cutting** — `Divider`/separator unification · no inline hex/style literals (Fill/Shape) · one label/row pattern.
- [ ] **Escalate (verify)** — `FillControls` `gradient.stops` `length ≥ 2` guard · emoji ↔ foreground-gradient z-order on publish · `content` schema-contract E2E coverage.
- [ ] **Captured** — SDK enum-display descriptor `enumOptions()` · UMD `React.*` sweep (`FillControls.tsx:42,65`).

---

## Backlog — later

- [ ] Emoji perf — code-split the ~215 KiB catalog + virtualize the grid (SDK-side).
- [ ] Photo support — `DesignPhotoOverlay` (a feature → version track, not polish).
